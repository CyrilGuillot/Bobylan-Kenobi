class TerminalGameConfig {

    constructor(text, words, correct_word, attempts) {
        this.text = text;
        this.words = words;
        this.correct_word = correct_word;
        this.attemps = attempts;
    }

}